import { Router } from "express";

const commentRouter = Router();

/*

commentRouter.post("/create", userGet);
commentRouter.put("/edit", userGet);
commentRouter.delete("/delete", userGet);

*/

export default commentRouter;
